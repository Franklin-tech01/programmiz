#include <stdio.h.>

int main()
{
    int age;
    double number;
    
/*size of fucntion used to know the size of a data type in c*/
    printf("int size = %zu", sizeof (age));
    printf("\ndouble size = %zu", sizeof (number));

    return 0;
}
